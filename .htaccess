RewriteEngine On

# Route storage requests to public/storage
RewriteRule ^storage/(.*)$ public/storage/$1 [L]

# Route build assets to public/build
RewriteRule ^build/(.*)$ public/build/$1 [L]

# Route everything else through Laravel front controller
RewriteRule ^(.*)$ public/$1 [L]
